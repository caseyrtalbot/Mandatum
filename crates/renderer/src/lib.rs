//! Placeholder terminal renderer boundary.
//!
//! Milestone 1 keeps rendering out of the core domain. Pane chrome, terminal
//! grid presentation, overlays, and frame timing are deferred.

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct RendererBoundary;
